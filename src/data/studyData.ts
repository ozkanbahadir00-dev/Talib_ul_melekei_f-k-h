// Sample updated content for src/data/studyData.ts

// Replace all instances of "always-visible" with "toggle" for answer reveals.
const studyData = [
    // Example of the modified data structures
    { revealMode: "toggle" }, // Changed from "always-visible" to "toggle"
    // ... other entries
];

export default studyData;